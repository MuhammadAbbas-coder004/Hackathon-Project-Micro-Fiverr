{
  "name": "micro-fiverr-root",
  "version": "1.0.0",
  "description": "Root package to run both frontend and backend",
  "scripts": {
    "dev:backend": "set NODE_OPTIONS=--dns-result-order=ipv4first && npm run dev --prefix back-end",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install-all": "npm install --prefix back-end && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
